services:
  hhru:
    build: .
    init: true
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    command:
      - sh
      - -c
      - >-
        while true; do
          hhru --headless run;
          sleep 14400;
        done
