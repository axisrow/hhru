"""Группы селекторов hh.ru по страницам.

Каждый модуль — селекторы одной страницы/экрана. Shim `selectors.py` переэкспортирует
их для обратной совместимости (`sel.VACANCY_CARD` и т.д.). Новый код должен импортировать
из конкретной группы.

Статус проверки (подтверждено curl-дампом / НЕ подтверждено) указан в каждом модуле.
"""

from __future__ import annotations
